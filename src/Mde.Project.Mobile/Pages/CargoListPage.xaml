<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    x:Class="Mde.Project.Mobile.Pages.CargoListPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:Mde.Project.Mobile.ViewModels"
    xmlns:models="clr-namespace:Mde.Project.Mobile.Models"
    x:DataType="viewmodels:CargoListViewModel"
    BackgroundColor="#1D3448" Opacity="1.4">

    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Image
                Grid.Row="0"
                Source="pff1.png"
                Aspect="AspectFit"
                HeightRequest="150"
                HorizontalOptions="Center" />

            <Border Grid.Row="1" Margin="0,25,0,12" StyleClass="baseBorder" >
                <ListView
                    x:Name="lstCargos"
                    HasUnevenRows="True"
                    ItemsSource="{Binding Cargos}"
                    Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Cargo">
                            <ViewCell Height="120">
                                <Border BackgroundColor="#1D3448" Opacity="0.7" Padding="25" Margin="10">
                                    <Grid ColumnDefinitions="Auto, *"
                                          RowDefinitions="Auto, Auto, Auto" >

                                        <Label Grid.Column="0" Grid.Row="0" Text="Total Weight:" 
                                               FontFamily="PlayBold" TextColor="White" />
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding TotalWeight}" 
                                               FontFamily="PlayBold" TextColor="White" />

                                        <Label Grid.Column="0" Grid.Row="1" Text="Is Dangerous:" 
                                               FontFamily="PlayBold" TextColor="White" />
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding IsDangerous}" 
                                               FontFamily="PlayBold" TextColor="White" />

                                        <Label Grid.Column="0" Grid.Row="2" Text="Destination:" 
                                               FontFamily="PlayBold" TextColor="White" />
                                        <Label Grid.Column="1" Grid.Row="2" Text="{Binding Destination}" 
                                               FontFamily="PlayBold" TextColor="White" />
                                    </Grid>
                                </Border>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </ScrollView>
</ContentPage>

